# Aegis

A reconnaissance tool for security researchers.

## What it does

- Takes a domain as input
- Finds subdomains via DNS lookups
- Scans common ports on each subdomain
- Outputs structured JSON

## Usage

```bash
python aegis.py google.com
